# Weather Service Agent

A simple agent that can determine the weather in Celsius or Fahrenheit for a select number of cities. Used to practice quickly building a system.

## Overview

Brief overview of the agent's purpose and main capabilities.

## Prerequisites

- Requirement 1
- Requirement 2

## Setup

1. Clone the repository and `cd` into the project directory.
2. (Optional) Create and activate a virtual environment, e.g. `python -m venv .venv` then `.venv\Scripts\activate` (Windows) or `source .venv/bin/activate` (Unix).
3. Install the package in editable mode so changes to the code take effect without reinstalling:
   ```bash
   pip install -e .
   ```
4. Configure environment (if needed).
5. Run the agent.

## Usage

How to run or interact with the agent.

## Configuration

Optional section for env vars, config files, or settings.

## License

[Specify license]
